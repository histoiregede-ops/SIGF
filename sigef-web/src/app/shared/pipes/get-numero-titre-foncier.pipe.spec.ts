import { GetNumeroTitreFoncierPipe } from './get-numero-titre-foncier.pipe';

describe('GetNumeroTitreFoncierPipe', () => {
  it('create an instance', () => {
    const pipe = new GetNumeroTitreFoncierPipe();
    expect(pipe).toBeTruthy();
  });
});
