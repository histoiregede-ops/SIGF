import { FormatPourcentagePipe } from './format-pourcentage.pipe';

describe('FormatPourcentagePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPourcentagePipe();
    expect(pipe).toBeTruthy();
  });
});
