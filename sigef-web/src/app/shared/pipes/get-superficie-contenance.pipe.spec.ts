import { GetSuperficieContenancePipe } from './get-superficie-contenance.pipe';

describe('GetSuperficieContenancePipe', () => {
  it('create an instance', () => {
    const pipe = new GetSuperficieContenancePipe();
    expect(pipe).toBeTruthy();
  });
});
