import { GetContenanceFromSuperficiePipe } from './get-contenance-from-superficie.pipe';

describe('GetContenanceFromSuperficiePipe', () => {
  it('create an instance', () => {
    const pipe = new GetContenanceFromSuperficiePipe();
    expect(pipe).toBeTruthy();
  });
});
