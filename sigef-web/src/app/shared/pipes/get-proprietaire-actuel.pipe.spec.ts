import { GetProprietaireActuelPipe } from './get-proprietaire-actuel.pipe';

describe('GetProprietaireActuelPipe', () => {
  it('create an instance', () => {
    const pipe = new GetProprietaireActuelPipe();
    expect(pipe).toBeTruthy();
  });
});
