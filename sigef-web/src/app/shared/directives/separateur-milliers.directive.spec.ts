import { SeparateurMilliersDirective } from './separateur-milliers.directive';

describe('SeparateurMilliersDirective', () => {
  it('should create an instance', () => {
    const directive = new SeparateurMilliersDirective();
    expect(directive).toBeTruthy();
  });
});
