import { FormatContactDirective } from './format-contact.directive';

describe('FormatContactDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatContactDirective();
    expect(directive).toBeTruthy();
  });
});
